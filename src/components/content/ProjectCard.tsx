import { Link } from 'react-router-dom'
import { Project } from '../../data/projects'
export function ProjectCard({ project }: { project: Project }) { return <article className="project-card" style={{ '--card-accent': project.accent } as React.CSSProperties}><div className="project-art" aria-hidden="true"><span className="art-code">{project.slug.slice(-2).toUpperCase()}</span><span className="art-shape" /></div><div className="project-card-body"><div className="card-meta"><span>{project.category}</span><span>{project.status}</span></div><h2>{project.name}</h2><p>{project.summary}</p><div className="tag-row">{project.stack.map((tag) => <span key={tag}>{tag}</span>)}</div><Link className="text-link" to={`/projects/${project.slug}`}>Open case file <span aria-hidden="true">↗</span></Link></div></article> }
